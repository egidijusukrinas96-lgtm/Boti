import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.adrenaline.wallet',
  appName: 'Adrenaline Wallet',
  webDir: 'dist'
};

export default config;
